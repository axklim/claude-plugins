# /merge Squash-by-Default Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Change `/merge`'s default merge strategy from rebase to squash, add `rebase`/`merge` override arguments, and reuse (or generate) the PR title/body for the squash commit message.

**Architecture:** Edit four markdown skill/doc files. The behavior change lives entirely in `plugins/dev-workflow/skills/merge/SKILL.md` (Task 1); the other three files are wording-only consistency edits so the lifecycle docs stop saying "rebase-merge" (Task 2). A final coherence pass greps for stale references (Task 3).

**Tech Stack:** Markdown skill files (Claude Code plugin "skills"). No build, no test harness — these files are read by the model at invocation time. Verification is grep + reading-level review, matching how the sibling skills are maintained.

## Global Constraints

- **No automated tests exist for skills.** "Verify" steps mean: re-read the edited section and/or run the specified `grep`. There is no test runner to invoke.
- **Spec is authoritative:** `docs/superpowers/specs/2026-06-27-merge-squash-default-design.md`. Every change below traces to it.
- **Squash strategy is the new default;** `rebase` and `merge` are opt-in via a single argument token, matched case-insensitively; empty/unrecognized → squash.
- **Squash message rule:** subject = PR title verbatim (never regenerated); body = PR body, or — only when the PR body is empty — a body generated by the `dev-workflow:commit-message` agent from `git diff "$base"...HEAD`, footer appended, written onto the PR via `gh pr edit` before merging. Title is always present, so it is never generated.
- **Strategy applies to the remote/PR path only.** The local-only fast-forward path (`git merge --ff-only`) is unchanged.
- **Footer string (exact), appended to any generated body:**
  ```
  🤖 Generated with [Claude Code](https://claude.com/claude-code)
  ```
- **Commit trailer** for every commit in this plan:
  ```
  Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
  ```
- Work happens on branch `feat/merge-squash-default` (already checked out; the spec is already committed there).

---

### Task 1: Rewrite `merge/SKILL.md` for squash-by-default

This is the only behavior change. Seven edits (A–G) to one file, then one commit. Apply them in order; each `old_string` is quoted verbatim from the current file.

**Files:**
- Modify: `plugins/dev-workflow/skills/merge/SKILL.md`

**Interfaces:**
- Consumes: the `dev-workflow:commit-message` agent (existing; used by `/restructure-commits`) — input is a diff range, output is a Conventional-Commits message with no footer/trailer.
- Produces: the user-facing `/merge` argument contract — `/merge` (squash), `/merge squash`, `/merge rebase`, `/merge merge`. Task 2's wording edits describe this same contract; keep the strategy names identical.

- [ ] **Step 1: Read the file**

Read `plugins/dev-workflow/skills/merge/SKILL.md` in full so every `old_string` below matches exactly (including indentation).

- [ ] **Step 2: Edit A — frontmatter `description`**

Replace:
```
  Merge the current feature branch's PR into main (rebase-merge) and clean up — update main,
```
with:
```
  Merge the current feature branch's PR into main (squash by default; override with `rebase`/`merge`)
  and clean up — update main,
```

- [ ] **Step 3: Edit B — intro sentence**

Replace:
```
Take a merge-ready branch (typically straight after `/restructure-commits`) the last mile: rebase-merge its
PR into `main`, then return the repo to a clean baseline.
```
with:
```
Take a merge-ready branch (typically straight after `/restructure-commits`) the last mile: merge its
PR into `main` — **squash by default**, or `rebase` / `merge` if you pass that argument — then return
the repo to a clean baseline.
```

- [ ] **Step 4: Edit C — add an `## Arguments` section**

Insert a new section immediately before the `## Workflow` heading. Replace:
```
## Workflow

### 1. Identify the branch, the trunk, and the PR
```
with:
```
## Arguments

One optional token selects the merge strategy (passed straight to `gh pr merge`). With no argument
it **squashes** — the new default:

| Invocation | Strategy |
|---|---|
| `/merge` | `--squash` (default) |
| `/merge squash` | `--squash` (explicit) |
| `/merge rebase` | `--rebase` — land the branch's commits verbatim |
| `/merge merge` | `--merge` — a merge commit |

Match the first token case-insensitively against `squash` / `rebase` / `merge`; anything empty or
unrecognized falls back to `squash`. The strategy governs the remote/PR path only — the local-only
path (Step 2) is always a fast-forward. Name the resolved strategy in the final report so the choice
is never silent.

## Workflow

### 1. Identify the branch, the trunk, and the PR
```

- [ ] **Step 5: Edit D — read the PR body in Step 1**

Replace:
```
[ -n "$HAS_REMOTE" ] && gh pr view --json number,state,title,url
```
with:
```
[ -n "$HAS_REMOTE" ] && gh pr view --json number,state,title,url,body
```

- [ ] **Step 6: Edit E — Step 2 remote-merge block**

Replace the entire current block:
```
**With a remote → merge the PR.** Merge straight away — **no confirmation prompt**. The
`/merge` invocation is the go-ahead, and the PR's approval/branch-protection state is GitHub's
call, so don't re-ask before merging:

```bash
gh pr merge <n> --rebase --delete-branch
```

- **Rebase-merge** lands the clean commits `/restructure-commits` produced directly on the trunk,
  with no merge commit. This pairs with `/restructure-commits`, which already rebased the branch
  onto the latest trunk. If your project squashes or uses merge commits instead, swap `--rebase`
  for `--squash` / `--merge`.
- `--delete-branch` removes the branch on the remote and locally (gh switches off it first).
- If GitHub **rejects** the merge — failing required checks, conflicts, branch-protection rules —
  stop and surface the reason. Don't reach for `--admin` or otherwise override GitHub's gate;
  whether to bypass it is the user's separate call.
```
with:
```
**With a remote → merge the PR.** Resolve the strategy from the argument (see Arguments) —
`squash` by default, else `rebase` / `merge`. Merge straight away — **no confirmation prompt**.
The `/merge` invocation is the go-ahead, and the PR's approval/branch-protection state is GitHub's
call, so don't re-ask before merging.

**For the default `squash`, the squash commit reuses the PR's own title and body** so the trunk
history stays as clean as a rebase-merge would. The PR title is always present; only the body can
be missing. If the PR body (from Step 1's `…,body`) is **empty**, generate one first rather than
let GitHub fall back to its commit-list default — and **never regenerate the title**:

1. Dispatch the **`commit-message`** agent (`subagent_type: dev-workflow:commit-message`) on the
   branch diff `git diff "$base"...HEAD`. Use its message as the body — prefer the body paragraphs;
   a subject-only result becomes a one-line body.
2. Append the footer:

   ```
   🤖 Generated with [Claude Code](https://claude.com/claude-code)
   ```
3. Write it onto the PR so the record matches the merge commit:

   ```bash
   gh pr edit <n> --body "<generated body>"
   ```

Then merge by the resolved strategy:

```bash
# squash (default) — PR body now guaranteed non-empty
gh pr merge <n> --squash --subject "<pr title>" --body "<pr body>" --delete-branch

# rebase — land the branch's commits verbatim (no --subject/--body)
gh pr merge <n> --rebase --delete-branch

# merge — a merge commit
gh pr merge <n> --merge --delete-branch
```

- **Squash (default)** collapses the branch to one commit on the trunk no matter how many commits
  it carries, so `/merge` no longer needs the branch pre-squashed — this is what makes merging
  without `/premerge` land cleanly. `--rebase` instead lands `/restructure-commits`'s clean commits
  verbatim; `--merge` makes a merge commit.
- The squash `--subject` is the PR title; GitHub may append the PR number `(#N)` to it (confirm in
  your setup — append `(#<n>)` to `--subject` yourself only if it doesn't).
- `--delete-branch` removes the branch on the remote and locally (gh switches off it first).
- If GitHub **rejects** the merge — failing required checks, conflicts, branch-protection rules —
  stop and surface the reason. Don't reach for `--admin` or otherwise override GitHub's gate;
  whether to bypass it is the user's separate call.
```

- [ ] **Step 7: Edit F — Step 6 report wording**

Replace:
```
- **With a remote:** PR merged (rebase), `<trunk>` updated, branch deleted (local + remote),
  backup tags cleared.
```
with:
```
- **With a remote:** PR merged (name the resolved strategy — squash / rebase / merge), `<trunk>`
  updated, branch deleted (local + remote), backup tags cleared.
```

- [ ] **Step 8: Edit G — Notes bullet on strategy**

Replace:
```
- This skill resolves the trunk automatically (`main` or `master`) and merges via rebase. If your
  project uses a different trunk name or merge strategy, adjust the `gh pr merge` strategy flag
  (`--squash` / `--merge`).
```
with:
```
- This skill resolves the trunk automatically (`main` or `master`) and merges via **squash by
  default** — pass `rebase` or `merge` to override per-merge. To change the default for your
  project, adjust the strategy resolution in Step 2.
```

- [ ] **Step 9: Verify the edits landed and the file is coherent**

Run:
```bash
grep -n "rebase-merge\|merged (rebase)\|merges via rebase\|--json number,state,title,url\b" plugins/dev-workflow/skills/merge/SKILL.md
```
Expected: **no output** (every stale phrase is gone; the `gh pr view` line now ends in `,body`).

Then run:
```bash
grep -n "squash\|--subject\|commit-message\|## Arguments" plugins/dev-workflow/skills/merge/SKILL.md
```
Expected: matches in the frontmatter, the new `## Arguments` section, and Step 2 (the three `gh pr merge` lines, the `commit-message` dispatch, and the `--subject`/`--body` squash command).

Read the `## Arguments` and Step 2 sections once more and confirm they read coherently together.

- [ ] **Step 10: Commit**

```bash
git add plugins/dev-workflow/skills/merge/SKILL.md
git commit -m "feat(dev-workflow): default /merge to squash with strategy override args

/merge now squash-merges by default; pass rebase or merge to override.
The squash commit reuses the PR title/body, generating a body via the
commit-message agent (and updating the PR) when the body is empty.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Consistency wording sweep (sibling skills + README)

Three wording-only edits so the rest of the dev-workflow docs reflect squash-by-default. No behavior change. The strategy names must match Task 1 exactly (`squash` / `rebase` / `merge`).

**Files:**
- Modify: `plugins/dev-workflow/skills/restructure-commits/SKILL.md`
- Modify: `plugins/dev-workflow/skills/premerge/SKILL.md`
- Modify: `plugins/dev-workflow/README.md`

**Interfaces:**
- Consumes: the argument contract Task 1 produced (the override names). Nothing produces anything for later tasks.

- [ ] **Step 1: Edit `restructure-commits/SKILL.md` — the "merge-ready" Note**

Replace:
```
- Hand the branch back **merge-ready** — `/merge` takes it from there: it merges the PR
  (rebase-merge) and cleans up the branch.
```
with:
```
- Hand the branch back **merge-ready** — `/merge` takes it from there: it merges the PR
  (squash by default) and cleans up the branch.
```

- [ ] **Step 2: Edit `restructure-commits/SKILL.md` — the trunk/strategy Note**

Replace:
```
- This skill resolves the trunk automatically (`main` or `master`, remote preferred over local)
  and merges via **rebase**. If your project uses a different trunk name or merge strategy,
  adjust steps 1–8 and the paired `/merge` skill accordingly.
```
with:
```
- This skill resolves the trunk automatically (`main` or `master`, remote preferred over local)
  and squashes the branch to a clean commit; `/merge` then squash-merges that PR by default. If
  your project uses a different trunk name or merge strategy, adjust steps 1–8 and the paired
  `/merge` skill accordingly.
```

- [ ] **Step 3: Edit `premerge/SKILL.md` — the trunk/strategy Note**

Replace:
```
- Resolves the trunk automatically (`main` or `master`) and merges via rebase — the same handling as
  the skills it chains. Adjust those skills if your project uses a different trunk name or merge strategy.
```
with:
```
- Resolves the trunk automatically (`main` or `master`); `/merge` squash-merges by default — the
  same handling as the skills it chains. Adjust those skills if your project uses a different trunk
  name or merge strategy.
```

- [ ] **Step 4: Edit `README.md` — the `merge` row**

Replace:
```
| `merge` | Takes a merge-ready branch the last mile: rebase-merges its PR into the trunk (`main`/`master`) — or, in a local-only repo, fast-forwards the local trunk to the branch — updates the trunk, deletes the branch, clears the `restructure-commits` backup tags, and prunes. |
```
with:
```
| `merge` | Takes a merge-ready branch the last mile: squash-merges its PR into the trunk (`main`/`master`) by default (override with `rebase`/`merge`) — or, in a local-only repo, fast-forwards the local trunk to the branch — updates the trunk, deletes the branch, clears the `restructure-commits` backup tags, and prunes. |
```

- [ ] **Step 5: Verify**

Run:
```bash
grep -rn -i "rebase-merge\|merges via rebase\|rebase-merges" plugins/dev-workflow/
```
Expected: **no output**.

- [ ] **Step 6: Commit**

```bash
git add plugins/dev-workflow/skills/restructure-commits/SKILL.md plugins/dev-workflow/skills/premerge/SKILL.md plugins/dev-workflow/README.md
git commit -m "docs(dev-workflow): align lifecycle docs with /merge squash default

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Final coherence verification

No edits expected — a repo-wide consistency check confirming nothing still describes `/merge` as rebase-merge. Fix inline only if something turns up.

**Files:**
- None (read-only), unless a stray reference needs fixing.

- [ ] **Step 1: Repo-wide grep for stale merge-strategy references**

Run:
```bash
grep -rn -i "rebase-merge\|merges via rebase\|rebase-merges its" . --include='*.md' | grep -v docs/superpowers/specs
```
Expected: **no output**. (The dated spec under `docs/superpowers/specs/` legitimately discusses the old behavior; everything else should be clean.)

- [ ] **Step 2: Confirm the argument contract is described consistently**

Run:
```bash
grep -rn -i "squash by default\|squash-merges\|/merge rebase\|/merge merge" plugins/dev-workflow/
```
Expected: matches in `merge/SKILL.md` (Arguments + Notes + description), `restructure-commits/SKILL.md`, `premerge/SKILL.md`, and `README.md` — all using the same `squash`/`rebase`/`merge` names.

- [ ] **Step 3: Read-through**

Read `plugins/dev-workflow/skills/merge/SKILL.md` start to finish once and confirm Steps 1–6 still flow (Step 1 reads `body`, Step 2 uses the strategy + message rule, Step 6 reports the strategy). If anything is incoherent, fix it and amend Task 1's commit:
```bash
git add plugins/dev-workflow/skills/merge/SKILL.md
git commit --amend --no-edit
```

- [ ] **Step 4: (Manual follow-up, not blocking) live walk**

A full live test needs a real GitHub PR and is left to the user post-merge: create a branch with several commits and an empty PR body, run `/merge`, and confirm the `commit-message` agent fills the PR body, the squash commit on the trunk matches it, and the branch is deleted. This is where the `(#N)`-suffix question (Step 6 of Task 1) gets settled empirically. Document the outcome if the `--subject` needs the explicit `(#<n>)`.

---

## Self-Review

**1. Spec coverage:**
- Default squash on remote path → Task 1 Edit E. ✓
- Strategy override args (`squash`/`rebase`/`merge`, case-insensitive, default squash) → Task 1 Edit C (Arguments) + Edit E (command). ✓
- Squash message: subject = PR title; body = PR body, or generated via `commit-message` agent when empty, footer appended, `gh pr edit` onto PR → Task 1 Edit E. ✓
- `body` added to `gh pr view --json` → Task 1 Edit D. ✓
- Report names the strategy → Task 1 Edit F. ✓
- Notes/description/intro updated → Task 1 Edits A, B, G. ✓
- Local-only path unchanged → no edit touches the local-only block; called out in Edit C/Global Constraints. ✓
- "PR must exist" precondition unchanged → no edit touches Step 1's PR check. ✓
- Consistency edits in restructure-commits, premerge, README → Task 2. ✓
- `(#N)` open question → Task 1 Edit E note + Task 3 Step 4. ✓
- Verification scenarios → Task 1 Step 9, Task 2 Step 5, Task 3 Steps 1–4. ✓

**2. Placeholder scan:** No "TBD"/"handle edge cases"/"similar to Task N". `<n>`, `<pr title>`, `<pr body>`, `<generated body>` are the skill's existing placeholder convention (the file already uses `<n>`, `<feature>`, `<trunk>`), reproduced verbatim into skill text — not plan placeholders. ✓

**3. Type/name consistency:** Strategy names `squash`/`rebase`/`merge` identical across Tasks 1–3; agent id `dev-workflow:commit-message` matches `/restructure-commits`; footer string identical to Global Constraints. ✓
